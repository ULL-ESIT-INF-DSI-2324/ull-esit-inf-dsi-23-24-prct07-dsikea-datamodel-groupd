/**
 * Interface to represent the data structure of a furniture
 */
export interface IFurniture {
  id: number;
  name: string;
  description: string;
  material: string;
  dimensions: string;
  price: number;
}